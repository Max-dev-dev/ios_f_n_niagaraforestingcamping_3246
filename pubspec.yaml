name: ios_f_n_niagaraforestcamping_3246
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # utils
  logger: ^2.5.0
  equatable: ^2.0.7
  freezed_annotation: ^2.4.4
  share_plus: ^10.1.4

  # maps
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # state management
  flutter_bloc: ^9.0.0

  # local data saving
  shared_preferences: ^2.5.2

  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  freezed: ^2.5.7
  json_serializable: ^6.9.0
  build_runner: ^2.4.13
flutter:
  uses-material-design: true

  assets:
   - assets/
   - assets/images/
